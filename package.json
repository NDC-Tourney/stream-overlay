{
  "name": "ndc-stream-overlay",
  "version": "1.10.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot --console-depth=0 src/main.ts",
    "start": "NODE_ENV=production bun src/main.ts",
    "build": "bun run build.ts",
    "prepare-pack:win": "vpk download github --repoUrl https://github.com/NDC-Tourney/stream-overlay -c win",
    "prepare-pack:linux": "vpk download github --repoUrl https://github.com/NDC-Tourney/stream-overlay -c linux",
    "prepare-pack": "bun run prepare-pack:win && bun run prepare-pack:linux",
    "pack:win": "vpk [win] pack -y -u NDC.StreamOverlay -v $(bun pm pkg get version | jq -r) -p dist/win -e ndc-stream-overlay.exe",
    "pack:linux": "vpk [linux] pack -y -u NDC.StreamOverlay -v $(bun pm pkg get version | jq -r) -p dist/linux -e ndc-stream-overlay",
    "pack": "bun run pack:win && bun run pack:linux",
    "upload-release:win": "vpk upload github --repoUrl https://github.com/ndc-tourney/stream-overlay --publish --tag $(git describe --tags) --targetCommitish $(git rev-parse HEAD) --token $(gh auth token) -c win",
    "upload-release:linux": "vpk upload github --repoUrl https://github.com/ndc-tourney/stream-overlay --publish --tag $(git describe --tags) --targetCommitish $(git rev-parse HEAD) --token $(gh auth token) -c linux --merge",
    "upload-release": "bun run upload-release:win && bun run upload-release:linux",
    "full-release-process": "git push && git push --tags && bun run build && bun run prepare-pack && bun run pack && bun run upload-release"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.90.2",
    "@tanstack/react-router": "^1.132.47",
    "bun-plugin-tailwind": "^0.0.15",
    "clsx": "^2.1.1",
    "dayjs": "^1.11.18",
    "immer": "^10.1.3",
    "motion": "^12.23.22",
    "nerdbank-gitversioning": "^3.8.118",
    "open": "^10.2.0",
    "partysocket": "^1.1.6",
    "react": "^19",
    "react-countdown": "^2.3.6",
    "react-dom": "^19",
    "tailwindcss": "^4.1.14",
    "velopack": "^0.0.1369-g1d5c984",
    "zod": "^4.1.12",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@eslint-react/eslint-plugin": "^2.0.6",
    "@eslint/js": "^9.37.0",
    "@tanstack/eslint-plugin-query": "^5.91.0",
    "@tanstack/react-query-devtools": "^5.90.2",
    "@types/bun": "^1.2.23",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9.37.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.0"
  }
}
